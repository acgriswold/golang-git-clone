package commands

const root = ".git-golang"
const objectsDirectory = root + "/objects"
const refsDirectory = root + "/refs"
const headFileName = root + "/HEAD"

const headFileContents = "ref: refs/heads/master\n"